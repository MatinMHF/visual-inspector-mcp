# visual-inspector-mcp
MCP server that lets Claude Code (or any MCP client) visually inspect rendered web pages via screenshots — navigate, click, resize, and read console logs through a persistent headless Chromium browser
